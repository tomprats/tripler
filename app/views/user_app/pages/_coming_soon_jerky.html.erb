<% classes = "col-md-4"
if index == count - 1 && (count%3) == 1
  classes += " col-md-offset-4"
elsif (index%2).zero? && (count%2).zero?
  classes += " col-md-offset-2"
end %>
<div class="<%= classes %> product-box">
  <%= image_tag product.image.url, class: "product-image" %>
</div>
